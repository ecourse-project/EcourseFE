import { useMemo } from 'react';
import { useStore } from "../store";
export var useAssetAwareness = function useAssetAwareness() {
  var presenceAsset = useStore(function (s) {
    return s.assetAwareness;
  });
  var internalUpdatePresenceAsset = useStore(function (s) {
    return s.internalUpdateAssetAwareness;
  });
  return useMemo(function () {
    return [presenceAsset, internalUpdatePresenceAsset];
  }, [presenceAsset]);
};