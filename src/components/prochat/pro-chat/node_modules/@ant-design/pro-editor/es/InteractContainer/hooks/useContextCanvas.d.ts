import type { ContextCanvasProps } from '../type';
export declare const useContextCanvas: ({ rules, status: outStatus, onStatusChange, getContainer, disabled, }: ContextCanvasProps) => {
    onClick: (e: MouseEvent) => void;
};
