import type { InternalProBuilderStore } from './createStore';
export declare const isDesignModeSelector: (s: InternalProBuilderStore) => boolean;
export declare const storeSelectors: {
    isDesignMode: (s: InternalProBuilderStore) => boolean;
};
