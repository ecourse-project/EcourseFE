import { getFlattenedData, getProjection } from "../utils/utils";
export var dataFlattenSelector = function dataFlattenSelector(s) {
  return getFlattenedData(s.treeData, s.activeId);
};
export var sortedIdsSelector = function sortedIdsSelector(s) {
  return dataFlattenSelector(s).map(function (_ref) {
    var id = _ref.id;
    return id;
  });
};
export var projectedSelector = function projectedSelector(s) {
  var activeId = s.activeId,
    overId = s.overId,
    offsetLeft = s.offsetLeft,
    indentationWidth = s.indentationWidth;
  return activeId && overId ? getProjection(dataFlattenSelector(s), activeId, overId, offsetLeft, indentationWidth) : null;
};