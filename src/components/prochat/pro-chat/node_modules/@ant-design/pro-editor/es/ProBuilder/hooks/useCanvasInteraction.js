import { useMemo } from 'react';
import { useStore } from "../store";
export var useCanvasInteraction = function useCanvasInteraction() {
  var interaction = useStore(function (s) {
    return s.interaction;
  });
  var internalUpdateCanvasInteract = useStore(function (s) {
    return s.internalUpdateCanvasInteract;
  });
  return useMemo(function () {
    return [interaction, internalUpdateCanvasInteract];
  }, [interaction]);
};