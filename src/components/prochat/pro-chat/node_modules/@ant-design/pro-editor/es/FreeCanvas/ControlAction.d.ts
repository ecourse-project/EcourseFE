import type { FC } from 'react';
import { Viewport } from 'reactflow';
interface ControlActionProps {
    viewport: Viewport;
    /**
     * 额外的操作按钮
     */
    extraBtns?: React.ReactNode[];
}
declare const ControlAction: FC<ControlActionProps>;
export default ControlAction;
