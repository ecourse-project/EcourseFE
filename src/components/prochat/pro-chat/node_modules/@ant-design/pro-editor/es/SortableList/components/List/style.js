import { createStyles } from "../../../theme";
export var useStyle = createStyles(function (_ref, _ref2) {
  var css = _ref.css,
    token = _ref.token,
    cx = _ref.cx,
    prefixCls = _ref.prefixCls;
  var horizontal = _ref2.horizontal;
  var prefix = "".concat(prefixCls, "-").concat(token.editorPrefix, "-sortable-list");
  return {
    container: cx("".concat(prefix, "-container"), css({
      listStyle: 'none',
      display: 'grid',
      gridAutoRows: 'max-content',
      gridGap: '2px',
      gridTemplateColumns: 'repeat(var(--columns, 1), 1fr)',
      width: '100%',
      margin: '0',
      padding: '0',
      borderRadius: '4px',
      transition: 'background-color 350ms ease',
      gridAutoFlow: horizontal ? 'column' : undefined
    }))
  };
});