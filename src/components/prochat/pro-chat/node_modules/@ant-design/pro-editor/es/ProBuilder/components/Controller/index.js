import { memo } from 'react';
import { isDesignModeSelector, useStore } from "../../store";

/**
 * Stage 下方的控制区域
 */
import { jsx as _jsx } from "react/jsx-runtime";
export var Controller = /*#__PURE__*/memo(function () {
  var componentAsset = useStore(function (s) {
    return s.componentAsset;
  });
  var isDesignMode = useStore(isDesignModeSelector);
  var Controller = isDesignMode ? componentAsset.DesignController : componentAsset.DevelopController;
  return Controller ? /*#__PURE__*/_jsx(Controller, {}) : null;
});