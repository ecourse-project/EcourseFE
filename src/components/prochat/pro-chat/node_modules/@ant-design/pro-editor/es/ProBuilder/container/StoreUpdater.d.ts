import { MutableRefObject } from 'react';
import { ProBuilderInstance } from '../hooks/useProBuilder';
import type { ProBuilderState } from '../store';
/**
 * 传入StoreUpdater 的值
 */
export interface StoreUpdaterProps extends Partial<ProBuilderState> {
    editorRef?: MutableRefObject<ProBuilderInstance>;
}
declare const StoreUpdater: import("react").MemoExoticComponent<({ config, onConfigChange, componentAsset, mode, onCanvasError, assetAwareness, onAssetAwarenessChange, editorAwareness, onEditorAwarenessChange, editorRef, }: StoreUpdaterProps) => any>;
export default StoreUpdater;
