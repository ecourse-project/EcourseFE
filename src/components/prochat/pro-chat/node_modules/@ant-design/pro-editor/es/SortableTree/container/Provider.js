import { createStore as _createStore, Provider, useStoreApi } from "../store";
import { Fragment as _Fragment } from "react/jsx-runtime";
import { jsx as _jsx } from "react/jsx-runtime";
export var SortableTreeProvider = function SortableTreeProvider(_ref) {
  var children = _ref.children,
    showDevtools = _ref.showDevtools;
  var isWrapped = true;
  var Content = /*#__PURE__*/_jsx(_Fragment, {
    children: children
  });
  try {
    useStoreApi();
  } catch (e) {
    isWrapped = false;
  }
  /* istanbul ignore if */
  if (isWrapped) {
    return Content;
  }
  return /*#__PURE__*/_jsx(Provider, {
    createStore: function createStore() {
      return _createStore(showDevtools);
    },
    children: Content
  });
};