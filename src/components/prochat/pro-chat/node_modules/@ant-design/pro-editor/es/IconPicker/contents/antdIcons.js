import * as AntdIcon from '@ant-design/icons';
var list = Object.keys(AntdIcon).filter(function (key) {
  return key.endsWith('Outlined') || key.endsWith('Filled');
});
var antdIconList = list.map(function (componentName) {
  return {
    type: 'antd',
    componentName: componentName
  };
});
export { antdIconList };
export default AntdIcon;