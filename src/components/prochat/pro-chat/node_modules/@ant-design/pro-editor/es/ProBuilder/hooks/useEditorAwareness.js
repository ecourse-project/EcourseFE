import { useDebounceFn } from 'ahooks';
import { useMemo } from 'react';
import { useStore } from "../store";
export var useUpdateEditorAwareness = function useUpdateEditorAwareness() {
  var updateEditorAwareness = useStore(function (s) {
    return s.internalUpdateEditorAwareness;
  });
  var _useDebounceFn = useDebounceFn(updateEditorAwareness, {
      wait: 100
    }),
    run = _useDebounceFn.run;
  return useMemo(function () {
    return {
      updateEditorAwareness: run
    };
  }, []);
};