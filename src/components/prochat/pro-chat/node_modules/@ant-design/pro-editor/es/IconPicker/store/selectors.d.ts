import type { IconUnit } from '../types';
import type { Store } from './store';
export declare const isEmptyIconfontScripts: (s: Store) => boolean;
export declare const selectedListSelector: (s: Store) => IconUnit[];
export declare const isEmptyIconList: (s: Store) => boolean;
export declare const displayListSelector: (s: Store) => IconUnit[];
export declare const iconfontScriptsChangeable: (s: Store) => boolean;
